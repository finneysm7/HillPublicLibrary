<h1>Hill Public Library</h1>

<ul>
	<% @books.each do |book| %>
	<li><%= link_to book.title, book_url(book) %>
		<%= book.author %><%= book.publication_date %><%= book.dewey_decimal_number %>
		<% if book.status %>
			checked out
		<%else%>
			checked in
		<%end%>
		<%= button_to "Remove Book", book_url(book), method: :delete %>
	</li>
	<%end%>
</ul>

<%= link_to "New Book", new_book_url%>